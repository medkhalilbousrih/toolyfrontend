import styled from "styled-components";

const CatalogueCards = () => {
  return (
    <>
      <CardContainer>aze</CardContainer>
    </>
  );
};

const CardContainer = styled.div``;

export default CatalogueCards;
